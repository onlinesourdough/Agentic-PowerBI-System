# Harness integration

- Pi: `.pi/settings.json` installs `agentic-powerbi` project-locally.
- Codex/other: read root `AGENTS.md` and use skill markdown as context.
- Claude/Copilot/Cursor: use skills/prompts as project context; avoid global setup unless intended.
